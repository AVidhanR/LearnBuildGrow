// create a tuple in ts
var user;
// order is very important
// can't assign un-orderly
user = ["Vidhan", 501, true];
// use case - for colors!
var rgb = [255, 256, 111];
var newUser = ["Vidhan", 1];
// can't do this!
// newUser.push(true);
newUser.push("Vidhan", "Reddy");
console.log("Can do this (V_V) => ", newUser);
